<header>

  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg  fixed-top">
    <div class="container">
      <a class="navbar-brand" href="/">HikeWith</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive"
        aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
        <span><i class="fas fa-align-justify"></i></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item active">
            <a class="nav-link" href="/">Home</a>
          </li>
          {{#if admin}}
          <li class="nav-item">
            <a class="nav-link" href="/Admin">Admin</a>
          </li>
          {{/if}}
          {{#if user}}
          <li class="nav-item">
            <a class="nav-link" href="MyHikes">My Hikes</a>
          </li>
          {{/if}}
          <li class="nav-item">
            <a class="nav-link" href="#contact">Contact</a>
          </li>
          {{#unless user}}
          <li class="nav-item">
            <a class="nav-link" href="/connexion">Login</a>
          </li>
          {{/unless}}
          {{#if user}}
          {{!-- <li class="nav-item">
            <form class="nav-form" method="post" action="/deconnexion">
              <input class="nav-button" type="submit" value="Déconnexion">
            </form>
          </li> --}}

          <li class="nav-item dropdown">
            <a href="" class="nav-link" data-toggle="dropdown" aria-expanded="false">{{user.nom_utilisateur}} </a>
            <ul class="dropdown-menu" role="menu">
              <li class="nav-item">
            <form class="nav-form" method="post" action="/deconnexion">
              <input class="nav-button" type="submit" value="Logout">
            </form>
          </li>

            </ul>
          </li>
          {{/if}}
        </ul>
      </div>

    </div>
  </nav>
</header>